import React from 'react'

/**
 * 
 * state
 * 1. describe state in react
 * 2. explore the features of state in react
 * 3. components can be stateful or stateless
 * 
 */
function ExampleFive(props) {
    return (
        <div>{props.message}</div>
    )
}

export default ExampleFive